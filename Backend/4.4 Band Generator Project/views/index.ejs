<%- include("partials/header.ejs") %>

  <!-- Step 2 - If an adjective and noun are sent to this EJS file, 
  change the h1 to show the adjective and noun. If not, show the default h1. -->
  <% if (locals.adjective && locals.noun) { %>
    <h1>
      <%= adjective %>
        <%= noun %>
    </h1>
    <% } else { %>
      <h1>Welcome to the Band Generator 🤘</h1>
      <% } %>

        <form action="/submit" method="POST">
          <input type="submit" value="Generate Name"><br />
          <button type="button" onclick="window.location.href='/'">Home</button>
        </form>

        <%- include("partials/footer.ejs") %>